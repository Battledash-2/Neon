const exp = {};

// probably gonna be moved to seperate files in the future
// ----------------------------------------------------------------

exp.Math = Math;
exp.process = process;

module.exports = exp;